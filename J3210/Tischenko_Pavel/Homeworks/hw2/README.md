# ДЗ2 — HTML-доступность



- Улучшены формы: явные `label` + `for`, `required`, `autocomplete`, корректные `type=submit/button`.
- Добавлен “skip link” для перехода к основному контенту.
- Добавлены статусы загрузки с `role="status"` и `aria-live="polite"` (каталог/алерты).
- Улучшена клавиатурная навигация: видимый `:focus-visible`, `tabindex="-1"` на `main` для skip-link.

## Проверка (как я проверял)

- Lighthouse → Accessibility: прогнать аудит, сохранить итоговый скор.

## Вывод

Проверил страницу в Chrome DevTools → Lighthouse (категория **Accessibility**).

- **Итог**: Accessibility **100** (скрин отчёта от `2026-03-18 11:53`).
- **Что находил Lighthouse и как исправил**:
  - **Недостаточная контрастность текста**: подтянул контраст для вторичного текста (`section-label`, `.text-muted`) и ссылок `.text-primary` через палитру CSS‑переменных.
  - **Одинаковые ссылки без различимого назначения** (“Открыть страницу курса”): добавил различимые `aria-label` с названием курса.

Дополнительно вручную проверил базовые вещи: навигацию с клавиатуры (Tab/Shift+Tab), видимый фокус и корректные подписи у полей форм.

